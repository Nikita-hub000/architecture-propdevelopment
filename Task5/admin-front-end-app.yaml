# Admin Front-end Service
# PropDevelopment - Task 5
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-front-end-app
  namespace: propdevelopment
  labels:
    app: admin-front-end-app
    role: admin-front-end
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-front-end-app
      role: admin-front-end
  template:
    metadata:
      labels:
        app: admin-front-end-app
        role: admin-front-end
    spec:
      containers:
      - name: nginx
        image: nginx:1.25-alpine
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: admin-front-end-app
  namespace: propdevelopment
  labels:
    app: admin-front-end-app
    role: admin-front-end
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: admin-front-end-app
    role: admin-front-end
